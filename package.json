{
  "name": "ai-launch-assistant",
  "version": "1.0.0",
  "scripts": {
    "install-client": "cd client && npm install",
    "build-client": "cd client && npm run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "@google/generative-ai": "^0.1.3"
  },
  "engines": {
    "node": "20.x"
  }
}